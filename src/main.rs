mod old;

#[allow(unused_variables)]
#[allow(dead_code)]

fn main() {
    let primes = [2, 3, 5, 7, 11];
    println!("{:?}", primes);

    let mut numbers = [0; 15];

    for number in numbers {
        println!("{}", number);
    }
}
